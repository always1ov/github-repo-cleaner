/** Static, English-first copy. Both locales render without client-side JavaScript. */
export const REPO = 'https://github.com/always1ov/github-repo-cleaner';
export const external = (url, text) => `<a href="${url}" target="_blank" rel="noopener noreferrer">${text}</a>`;
const tokenLink = external('https://github.com/settings/personal-access-tokens/new', 'Create a fine-grained token');
const classicLink = external('https://github.com/settings/tokens', 'classic token settings');
const restoreLink = external('https://docs.github.com/en/repositories/creating-and-managing-repositories/restoring-a-deleted-repository', 'GitHub’s repository recovery documentation');
const backupLink = external('https://docs.github.com/en/repositories/archiving-a-github-repository/backing-up-a-repository', 'GitHub’s backup guide');
const packageLink = external('https://docs.github.com/en/packages/learn-github-packages/deleting-and-restoring-a-package', 'GitHub Packages documentation');
export const locales = {
  en: {
    code: 'en', other: '中文', navGuide: 'How to use', navSecurity: 'Security', open: 'Open cleaner', demo: 'Try the demo', free: 'Free & open source', source: 'Source', privacy: 'Privacy', terms: 'Terms', guide: 'Guide', skip: 'Skip to content',
    description: 'A free, open-source tool to find old GitHub repositories, review your selection, and archive or delete in bulk. No sign-up. No subscription.',
    badge: 'A small tool for a tidier GitHub', title: 'Clean up your GitHub.<br><span>Keep what matters.</span>', intro: 'Old forks. Weekend experiments. Repositories you forgot you had. Find the clutter, review your choices, and make a little room.',
    fine: 'No sign-up. No subscription. All features are free.', sample: 'Sample data', workspace: 'Your repositories', previewFilter: 'Old forks', previewAll: 'All repositories', previewEmpty: 'Empty repos', previewSelected: '2 repositories selected', previewArchive: 'Review & archive', previewCaption: 'Nothing happens until you review and confirm.',
    previewMetrics: ['repositories', 'old forks', 'archived'], proof: ['Runs in your browser', 'Token sent only to GitHub', 'Review before every action', 'MIT licensed'],
    featuresKicker: 'Less clutter, more clarity', featuresTitle: 'One job. Done thoughtfully.', featuresIntro: 'Not another dashboard to maintain. Just a useful little tool when your account needs a tidy-up.',
    features: [['Find the forgotten', 'Filter by age, owner, visibility, or fork status. See what you have before deciding what to do with it.'], ['Archive before deleting', 'Keep projects read-only when you are not ready to let go. Unarchive them later. Delete only what you choose.'], ['Leave with a record', 'Export a CSV or JSON list, generate a Git backup script, and download the results of your cleanup.']],
    stepsKicker: 'A few deliberate steps', stepsTitle: 'You stay in control.', steps: [['Connect your account', 'Use a short-lived token with access only to the repositories you need. Or start with the demo.'], ['Find and review', 'Choose your filters, select repositories, and read the exact list each action will affect.'], ['Confirm the cleanup', 'Archive or delete. Destructive actions require an acknowledgement and a typed confirmation.']],
    securityKicker: 'Trust, without the hand-waving', securityTitle: 'Your token goes to GitHub.<br>Not to our server.', securityText: 'The app calls GitHub directly from your browser. It does not save your token, run analytics, or load third-party scripts. Inspect the source before connecting.', securityLink: 'Read the security model →', browser: 'Your browser', browserNote: 'Token held in memory', githubNote: 'Authenticated API requests', flowNote: 'This reduces exposure; it is not a guarantee against a compromised site, browser extension, or mistaken deletion.',
    faqTitle: 'A few things worth knowing.', faq: [
      ['Is everything really free?', 'Yes. There is no paid tier, trial timer, usage paywall, or subscription in this tool. GitHub’s own permissions and API limits still apply.'],
      ['Do I need to give you my GitHub password?', 'No. The tool uses a personal access token, not your password. Tokens can carry powerful permissions: use selected repositories, a short expiry, and revoke the token when you finish.'],
      ['Can I try it without connecting?', 'Yes. The demo uses made-up repositories and packages. Its actions change demo data only and make no API requests.'],
      ['Does “old” mean safe to delete?', 'No. Age, stars, size, and fork status are filters, not proof that a repository is disposable. Check dependencies and back up important data yourself.'],
      ['Can I undo a deletion?', 'There is no undo in this tool. GitHub may restore eligible repositories within 90 days, subject to restrictions. Do not count on recovery: archive when unsure and verify your backup before deleting.'],
      ['What about packages and other actions?', 'Package pruning, transfers, and visibility changes are still available in the workspace. They may require additional permissions. A Git mirror script is not a complete backup of GitHub issues, releases, settings, or package data.']
    ],
    bottomTitle: 'A little less clutter starts here.', bottomText: 'Take a look around with sample data. Connect only when you are ready.', footerNote: 'An independent, free tool. Not affiliated with or endorsed by GitHub.',
    documents: {
      guide: { title: 'A careful cleanup, step by step.', intro: 'Start with the demo. Connect only after you understand the permissions and the actions you plan to take.', body: `
<h2>1. Try the demo first</h2><p>The demo uses sample repositories and packages. Filtering, selection, confirmation dialogs, and result reports work against that sample data. No GitHub API request is made in demo mode.</p>
<h2>2. Create a short-lived token</h2><p>For repository-only cleanup, use a fine-grained personal access token where your account and organization allow it. Select the correct resource owner, choose <strong>Only select repositories</strong>, and include only repositories you intend to review. Set the shortest practical expiration.</p><p>${tokenLink}. Metadata read access allows listing selected repositories. Archiving, changing visibility, and deletion require <strong>Repository permissions → Administration → Read and write</strong>. That is a powerful permission, not a harmless sign-in. Organization policies or approval requirements may further limit access.</p>
<div class="callout"><p><strong>Do not grant every permission just to get started.</strong> Read-only browsing does not require permission to delete. Add write permissions only for the actions you decide to use. The app cannot infer all fine-grained permissions from a token’s format; GitHub makes the final authorization decision.</p></div>
<h3>Classic tokens and advanced tools</h3><p>For a classic token, <code>public_repo</code> supports public-repository access, while <code>repo</code> includes private repositories. Deleting repositories also needs <code>delete_repo</code>. Organization access may need <code>read:org</code> and SSO authorization. Grant only the scopes you need through ${classicLink}.</p><p>GitHub Packages currently requires a classic token. Reading packages needs <code>read:packages</code>; deletion also needs <code>delete:packages</code> and appropriate package permissions. Transfers have separate token and ownership restrictions; a fine-grained PAT does not support every advanced endpoint. See ${packageLink} and ${external('https://docs.github.com/en/rest/repos/repos#transfer-a-repository', 'the transfer API documentation')}.</p>
<h2>3. Load and filter</h2><p>Paste the token into the workspace and choose <strong>Load repositories</strong>. The token is sent to GitHub in the Authorization header. Search names and descriptions, or filter by owner, visibility, age, and kind. Selecting all matching items applies only to the loaded list, not unseen repositories.</p><p>A warning appears when pagination reaches the safety cap. Filters cannot retrieve items that were never loaded. A limited listing is not a complete account inventory.</p>
<h2>4. Review, then act</h2><p>Prefer <strong>Archive</strong> when unsure. For deletion, inspect the exact repository list, acknowledge the risk, and type the requested confirmation. Stars and activity are context, not a safety score. Check that no deployment, dependency, or collaborator still needs these projects.</p>
<h2>5. Verify a backup yourself</h2><p><strong>Generate Git backup script</strong> creates commands; it does not run or verify a backup. A <code>git clone --mirror</code> copies Git history and refs, not the complete GitHub project. Git LFS objects, a separate wiki repository, issues, pull requests, release assets, secrets, settings, and packages require separate handling. Read ${backupLink}.</p>
<h2>6. Save the result and disconnect</h2><p>Download the run report. Stopping a batch prevents remaining work where possible; it does not roll back completed requests. Disconnect to clear the app’s session state, then revoke your token on GitHub. Closing or refreshing the app also discards its in-memory token.</p><p>There is no undo in this tool. GitHub can restore some eligible deleted repositories within 90 days, with restrictions. Read ${restoreLink}; never rely on recovery instead of a verified backup.</p>` },
      security: { title: 'Know what you are trusting.', intro: 'A token that can administer repositories deserves a clear explanation, not an absolute safety claim.', body: `
<h2>What the app does</h2><p>The deployed workspace is a self-contained HTML file. Its JavaScript calls <code>https://api.github.com</code> directly from the browser. There is no application backend, login service, database, payment system, analytics SDK, or session recording.</p>
<h2>Where your token goes</h2><p>The app sends your token to GitHub as an Authorization header. It does not intentionally send the token to the website host or store it in localStorage, sessionStorage, cookies, IndexedDB, or URLs. While you are typing, the token is also present in the password input; connecting, entering the demo, or disconnecting clears that input in the published app.</p><p>The browser stores only language, theme, and page-size preferences locally. The app keeps repository data and result reports in memory unless you explicitly export them.</p>
<h2>Browser-enforced restrictions</h2><p>A Content Security Policy denies network destinations by default and limits application API connections to <code>api.github.com</code>. Scripts are inlined and hash-authorized at build time; no remote fonts, images, scripts, styles, or frames are needed by the app. Source-level checks additionally reject requests to other origins.</p>
<div class="callout"><p><strong>CSP is a layer of defense, not proof of absolute safety.</strong> An attacker who controls the deployed HTML can change its policy and code. Browser extensions, malware, compromised dependencies in a publisher’s build environment, and your own decisions remain outside this guarantee.</p></div>
<h2>Destructive actions are deliberate</h2><p>Each action shows the affected list before executing. Deletion requires an acknowledgement and typed confirmation. The app offers no rollback. A stale repository or a generated backup script is not evidence that deletion is safe.</p>
<h2>Reduce your exposure</h2><p>Review the source, select only necessary repositories, use a short expiry, prefer archiving, and revoke the token afterwards. The build output at <code>dist/app/index.html</code> can be saved and opened locally. Its interface and demo work offline; live GitHub operations need a network connection.</p>
<h2>Report a problem carefully</h2><p>${external(REPO + '/issues', 'Open an issue')} without sharing tokens, credentials, private repository names, or exploit details that put users at immediate risk. For a sensitive report, request a private reporting route first. This independent project does not have a staffed security response service.</p>` },
      privacy: { title: 'A small, straightforward data footprint.', intro: 'This page describes the deployed free tool, not GitHub’s services or your hosting provider’s infrastructure.', body: `
<h2>What this application does not collect</h2><p>There are no application accounts, billing records, advertising identifiers, analytics events, or session recordings. The application has no backend database for tokens or repository lists.</p>
<h2>What your browser and GitHub handle</h2><p>Your token and repository data are held in browser memory during use. The app sends authenticated requests directly to GitHub. GitHub receives the token, requested actions, and normal connection information under ${external('https://docs.github.com/en/site-policy/privacy-policies/github-general-privacy-statement', 'GitHub’s privacy statement')}.</p><p>Language, theme, and page-size preferences are saved in localStorage under the <code>grc.</code> prefix. Clear the site’s browser storage to remove them. Disconnecting or closing the page clears the app’s in-memory session; exported files remain wherever you save them.</p>
<h2>Hosting and external links</h2><p>The static website host still receives ordinary page requests and may process IP addresses, user-agent information, and access/security logs under its own policies. “No analytics” does not mean that no infrastructure logs exist. Following a link to GitHub or another website takes you to that service.</p>
<h2>Reports and exports</h2><p>CSV, JSON, backup scripts, and result reports can contain repository names and other account metadata. They are generated locally when requested. Review them before sharing. Never attach a token or sensitive repository data to a public issue.</p>
<h2>Changes and questions</h2><p>The source and changes are public in ${external(REPO, 'the project repository')}. Privacy questions can be raised through ${external(REPO + '/issues', 'GitHub issues')} without including sensitive data.</p>` },
      terms: { title: 'Free to use. Yours to decide.', intro: 'A practical summary of how this independent, open-source utility is offered.', body: `
<h2>No paid tier</h2><p>This version of Repo Cleaner is free. It has no subscription, checkout, or paid feature gate. GitHub’s own service terms, permissions, and rate limits still apply.</p>
<h2>Only act on repositories you are authorized to manage</h2><p>You are responsible for reviewing selections, permissions, backups, and the consequences of actions taken with your token. Do not use the tool to access or modify accounts, repositories, or packages without authorization.</p>
<h2>No promised recovery or availability</h2><p>The tool does not provide rollback, guaranteed backups, uptime commitments, or a managed recovery service. Interrupting an operation does not reverse requests already processed by GitHub. Archive rather than delete when uncertain.</p>
<h2>Open-source license</h2><p>The software is available under the ${external(REPO + '/blob/fe703e5082d26524d8f548ad9dd105114b25dc34/LICENSE', 'MIT license')}, including its warranty disclaimer. This page does not replace the license or GitHub’s terms. Repo Cleaner is not affiliated with or endorsed by GitHub.</p>` }
    }
  },
  zh: {
    code: 'zh-CN', other: 'English', navGuide: '使用指南', navSecurity: '安全说明', open: '打开清理工具', demo: '体验演示', free: '免费 · 开源', source: '源代码', privacy: '隐私', terms: '使用说明', guide: '指南', skip: '跳转到正文',
    description: '免费开源的 GitHub 仓库整理工具。筛选旧项目，逐项确认，再批量归档或删除。无需注册，没有订阅。',
    badge: '让 GitHub 清爽一点的小工具', title: '整理你的 GitHub。<br><span>留下真正重要的。</span>', intro: '很久以前的 Fork、周末写的实验、已经忘记的项目。先看清楚有哪些，再决定保留、归档还是删除。', fine: '无需注册，没有订阅，全部功能免费。', sample: '演示数据', workspace: '你的仓库', previewFilter: '旧 Fork', previewAll: '全部仓库', previewEmpty: '空仓库', previewSelected: '已选择 2 个仓库', previewArchive: '检查并归档', previewCaption: '只有经过你的检查与确认，操作才会执行。', previewMetrics: ['个仓库', '个旧 Fork', '个已归档'], proof: ['在浏览器中运行', 'Token 仅发送给 GitHub', '每次操作前都要确认', 'MIT 开源协议'],
    featuresKicker: '少一点杂乱，多一点清楚', featuresTitle: '做好一件小事。', featuresIntro: '不是又一个需要维护的管理平台，只在你想整理账号时提供帮助。', features: [['找出遗忘的项目', '按时间、所属账号、可见性和 Fork 状态筛选。先了解现状，不替你判断什么该删除。'], ['不确定，就先归档', '让旧项目保持只读，需要时可以取消归档。只有明确选择的项目才会被删除。'], ['留下一份操作记录', '导出 CSV 或 JSON 清单，生成 Git 备份脚本，并保存本次整理的执行报告。']],
    stepsKicker: '每一步，都由你决定', stepsTitle: '控制权始终在你手里。', steps: [['连接账号', '使用有效期较短、只允许访问指定仓库的 Token。也可以先体验无需授权的演示。'], ['筛选与检查', '设置筛选条件，选择仓库，并逐项检查每次操作将影响的准确列表。'], ['确认后执行', '决定归档还是删除。危险操作需要勾选风险确认，并输入指定的确认文字。']],
    securityKicker: '把信任说清楚', securityTitle: 'Token 发给 GitHub，<br>不经过我们的服务器。', securityText: '工具在浏览器里直接调用 GitHub API，不保存 Token，不加载统计分析或第三方脚本。连接前，你可以检查源代码。', securityLink: '阅读安全模型 →', browser: '你的浏览器', browserNote: 'Token 保留在内存中', githubNote: '带身份验证的 API 请求', flowNote: '这些措施可以降低风险，但无法保证抵御被篡改的页面、恶意浏览器扩展或误删。',
    faqTitle: '开始前，了解这几件事。', faq: [
      ['真的全部免费吗？', '是的。没有付费套餐、试用倒计时、功能付费墙或订阅。GitHub 自身的权限和 API 限制仍然适用。'],
      ['需要提供 GitHub 密码吗？', '不需要。工具使用个人访问令牌，而不是密码。Token 可能具有很高权限，请限制仓库范围、设置较短有效期，并在用完后撤销。'],
      ['不连接账号也能试用吗？', '可以。演示模式使用虚构仓库与软件包，只修改演示数据，不会向 GitHub 发出 API 请求。'],
      ['旧仓库就可以安全删除吗？', '不能这样判断。时间、星标、体积和 Fork 状态只是筛选条件，不代表项目没有价值。请自己检查依赖关系并备份重要数据。'],
      ['删除后能撤销吗？', '本工具没有撤销功能。GitHub 对部分符合条件的仓库提供 90 天内恢复，但存在限制。不要依赖恢复功能，不确定时先归档。'],
      ['原来的高级功能还在吗？', '软件包清理、转移和可见性修改仍在工作台中，需要相应权限。Git 镜像脚本不等于对 Issues、发布附件、设置或软件包的完整备份。']
    ], bottomTitle: '从一次小小的整理开始。', bottomText: '先用演示数据体验，准备好后再连接账号。', footerNote: '独立开发的免费工具，与 GitHub 无隶属或背书关系。',
    documents: {
      guide: { title: '一步一步，谨慎整理。', intro: '先体验演示，理解权限和操作影响后，再连接真实账号。', body: `
<h2>1. 先体验演示</h2><p>演示使用虚构的仓库与软件包。筛选、选择、确认弹窗和操作报告都可以体验，不会向 GitHub 发送 API 请求。</p>
<h2>2. 创建短期 Token</h2><p>只整理仓库时，优先使用你的账号与组织允许的 fine-grained Token。选择正确的资源所有者，使用 <strong>Only select repositories</strong>，只勾选准备检查的仓库，并设置尽可能短的有效期。</p><p>${external('https://github.com/settings/personal-access-tokens/new', '在 GitHub 创建细粒度 Token')}。Metadata 读取权限用于列出选定仓库；归档、修改可见性与删除需要 <strong>Administration → Read and write</strong>。这是一项高权限，不是普通登录。组织策略、审批或 SSO 也可能影响访问。</p>
<div class="callout"><p><strong>不要为了开始使用就勾选全部权限。</strong>只读浏览不需要删除权限，确定操作后再增加所需权限。工具无法仅凭 Token 格式识别所有细粒度权限，最终授权结果由 GitHub 决定。</p></div>
<h3>Classic Token 与高级功能</h3><p>Classic Token 的 <code>public_repo</code> 用于公开仓库，<code>repo</code> 覆盖私有仓库，删除另需 <code>delete_repo</code>。组织访问可能需要 <code>read:org</code> 与 SSO 授权。${external('https://github.com/settings/tokens', '打开 Classic Token 设置')}，只授予所需权限。</p><p>GitHub Packages 当前要求 Classic Token，读取需要 <code>read:packages</code>，删除另需 <code>delete:packages</code> 和对应软件包权限。转移还有独立的 Token 与所有权限制，fine-grained PAT 并不支持所有高级接口。查看 ${external('https://docs.github.com/en/packages/learn-github-packages/deleting-and-restoring-a-package', 'Packages 官方说明')} 和 ${external('https://docs.github.com/en/rest/repos/repos#transfer-a-repository', '转移接口说明')}。</p>
<h2>3. 加载与筛选</h2><p>在工作台粘贴 Token，点击“加载仓库”。Token 通过 Authorization 请求头直接发送给 GitHub。搜索名称或描述，也可以按所属账号、可见性、更新时间与类型筛选。“选择已加载的匹配项”只作用于当前加载的数据。</p><p>达到分页安全上限时会出现警告。筛选不能补全从未加载的仓库，有限列表不能被当作完整账号清单。</p>
<h2>4. 检查后再执行</h2><p>不确定时优先归档。删除前请逐项检查仓库列表，勾选风险确认并输入要求的文字。星标与活跃度不是安全评分，请确认部署、依赖与协作者不再需要这些项目。</p>
<h2>5. 自己执行并验证备份</h2><p>“生成 Git 备份脚本”只生成命令，不执行也不验证备份。<code>git clone --mirror</code> 复制 Git 历史和引用，不是完整的 GitHub 项目备份。LFS 对象、独立 Wiki、Issues、PR、发布附件、密钥、设置和软件包需要另行处理。${external('https://docs.github.com/en/repositories/archiving-a-github-repository/backing-up-a-repository', '查看官方备份指南')}。</p>
<h2>6. 保存报告并断开连接</h2><p>下载执行报告。停止批处理不代表撤销已经完成的请求。断开连接清除会话状态后，请到 GitHub 撤销 Token。关闭或刷新页面也会丢弃内存中的 Token。</p><p>本工具没有撤销功能。GitHub 可在 90 天内恢复部分符合条件的仓库，但存在限制。${external('https://docs.github.com/en/repositories/creating-and-managing-repositories/restoring-a-deleted-repository', '阅读官方恢复说明')}，不要用恢复承诺代替经过验证的备份。</p>` },
      security: { title: '知道自己在信任什么。', intro: '能够管理仓库的 Token，值得一份准确的解释，而不是绝对安全的承诺。', body: `
<h2>工具如何运行</h2><p>发布后的工作台是一个自包含 HTML 文件，JavaScript 在浏览器里直接请求 <code>https://api.github.com</code>。没有应用后端、登录服务、数据库、支付系统、统计 SDK 或会话录制。</p>
<h2>Token 去了哪里</h2><p>Token 通过 Authorization 请求头发送给 GitHub。应用不会主动将它发送给网站托管方，也不会写入 localStorage、sessionStorage、Cookie、IndexedDB 或 URL。输入期间 Token 也存在于密码输入框；发布版在连接、进入演示或断开连接时清空输入框。</p><p>浏览器只持久保存语言、主题和每页条数。仓库数据与执行报告保留在内存中，除非你主动导出。</p>
<h2>浏览器实施的限制</h2><p>内容安全策略默认拒绝网络来源，只允许应用 API 连接到 <code>api.github.com</code>。脚本内联并使用构建时哈希授权，应用不需要远程字体、图片、脚本、样式或框架。代码还会主动拒绝向其他来源发起请求。</p>
<div class="callout"><p><strong>CSP 是一层防护，不是绝对安全的证明。</strong>能修改发布 HTML 的攻击者也能修改策略。恶意浏览器扩展、系统恶意软件、构建环境被入侵以及误操作，并不由这些措施完全覆盖。</p></div>
<h2>危险操作需要明确确认</h2><p>每次操作都会先展示影响列表。删除需要风险勾选和文字确认，工具不提供回滚。仓库久未更新或已生成备份脚本，都不是可以安全删除的证据。</p>
<h2>降低暴露范围</h2><p>检查源代码，只授权必要仓库，使用较短有效期，优先归档，用完后撤销 Token。构建结果 <code>dist/app/index.html</code> 可以保存到本地直接打开。界面与演示支持离线运行，真实 GitHub 操作仍需联网。</p>
<h2>报告问题</h2><p>${external(REPO + '/issues', '提交 Issue')} 时不要包含 Token、凭据、私有仓库名称或会立即危及用户的利用细节。敏感问题请先询问私下沟通渠道。本独立项目没有专职安全响应团队。</p>` },
      privacy: { title: '尽量简单的数据足迹。', intro: '这里说明的是发布版免费工具，不替代 GitHub 或托管平台自身的隐私政策。', body: `
<h2>应用不收集什么</h2><p>没有应用账号、账单记录、广告标识符、统计事件或会话录制，也没有用来保存 Token 与仓库列表的后端数据库。</p>
<h2>浏览器与 GitHub 处理什么</h2><p>Token 和仓库数据在使用期间保留在浏览器内存中。工具直接向 GitHub 发送带身份验证的请求。GitHub 接收 Token、操作请求及普通连接信息，受 ${external('https://docs.github.com/en/site-policy/privacy-policies/github-general-privacy-statement', 'GitHub 隐私政策')} 约束。</p><p>语言、主题和分页偏好通过 <code>grc.</code> 前缀存入 localStorage，清除网站存储即可删除。断开连接或关闭页面会清除应用的内存会话；已导出的文件仍保留在你选择的保存位置。</p>
<h2>托管与外部链接</h2><p>静态网站托管方仍然会接收普通页面请求，并可能按照自己的政策处理 IP 地址、浏览器信息以及访问和安全日志。“没有统计分析”不代表基础设施不存在日志。点击外部链接后适用相应服务的规则。</p>
<h2>报告与导出</h2><p>CSV、JSON、备份脚本和执行报告可能包含仓库名称等账号信息，它们在请求时由本地生成。分享前请检查内容，不要把 Token 或敏感仓库数据附在公开 Issue 中。</p>
<h2>变更与咨询</h2><p>代码与变更记录公开在 ${external(REPO, '项目仓库')}。隐私问题可通过 ${external(REPO + '/issues', 'GitHub Issues')} 提出，请勿包含敏感数据。</p>` },
      terms: { title: '免费使用，谨慎决定。', intro: '关于这个独立开源小工具的实用说明。', body: `
<h2>没有付费套餐</h2><p>当前版本免费，没有订阅、结算或付费功能门槛。GitHub 自身的服务条款、权限和速率限制仍然适用。</p>
<h2>仅管理你有权管理的资源</h2><p>请自行检查所选仓库、权限、备份与操作后果。不要使用工具访问或修改未经授权的账号、仓库或软件包。</p>
<h2>不承诺恢复与可用性</h2><p>工具不提供回滚、保证成功的备份、可用性承诺或托管恢复服务。中断操作不会撤销 GitHub 已经处理的请求。不确定时请归档而不是删除。</p>
<h2>开源许可</h2><p>软件按 ${external(REPO + '/blob/fe703e5082d26524d8f548ad9dd105114b25dc34/LICENSE', 'MIT 许可证')} 提供，包括其中的免责声明。本页面不替代该许可证或 GitHub 自身条款。Repo Cleaner 与 GitHub 无隶属或背书关系。</p>` }
    }
  }
};
