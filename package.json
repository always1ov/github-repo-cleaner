{
  "name": "github-repo-cleaner",
  "version": "1.0.0",
  "private": true,
  "description": "Bulk-manage your GitHub repositories and packages from a single HTML page that can only talk to api.github.com.",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/always1ov/github-repo-cleaner.git" },
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:browser": "npm install --no-save playwright && npx playwright install --with-deps chromium && node --test test/browser.test.mjs"
  },
  "engines": { "node": ">=20" }
}
